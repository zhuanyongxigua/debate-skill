# Eval fixture repo

A tiny placeholder repository so the eval's debate has a valid working directory
(the runner's allowlist requires `repo` to be a real directory under a repo
root). The sample debate question is repo-independent; this repo just needs to
exist.
