{
  "id": "rate-limiter",
  "category": "concurrency_state",
  "difficulty": "medium",
  "is_simple": false,
  "bug_description": "RateLimiter uses a single global list instead of a per-user dict, so any user exhausting the limit blocks all users.",
  "true_root_cause": "self._log is a shared list; requests from all users accumulate together. Fix: use a per-user dict of timestamp lists.",
  "seeded_files": ["src/limiter.py"],
  "expected_methods": ["multi-candidate-analysis", "work-gate"],
  "avoid_methods": ["structured-debate"],
  "public_tests": ["tests/test_limiter.py::test_per_user_isolation"],
  "hidden_tests": ["tests/test_limiter.py::test_per_user_isolation"],
  "test_command": "pytest tests/ -v",
  "tests_that_fail_with_bug": ["test_per_user_isolation"],
  "tests_that_pass_with_bug": ["test_basic_limit", "test_window_expiry"]
}
